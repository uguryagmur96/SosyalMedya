server:
  port: 8888

spring:
  application:
    name: config-server-git
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/uguryagmur96/java8configrepo.git"
          default-label: main
          skip-ssl-validation: true
          username: uguryagmur96
          password: ${GIT_PASSWORD}